extends layout.pug

block main
  h1 New Book

  include form-error

  form(action="/new", method="post")
    include form
    p
      input(type="submit" value="Create New Book")
    p
      a.button(href='/books') Cancel
